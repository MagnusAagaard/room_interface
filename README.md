# room_interface

Room interface package that is (was) used to show the first three rooms in the queue in Rviz for manual navigation purposes. Also contains code that sends the target navigation pose to the ROS navigation stack, with hardcoded locations of buttons for demostrating purposes
